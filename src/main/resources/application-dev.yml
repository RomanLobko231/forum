spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/forumdb
    username: forum_user
    password: forum
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect